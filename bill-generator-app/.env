# --- Configuration ---
# 1. Database Location (Private IP of Instance 3)
DB_HOST=10.0.0.30 # Change to the private IP of your database instance

PUBLIC_IP = 15.206.187.192 # Change to the public IP of your gateway instance

# 2. Public Access Point (Public IP/Domain of Instance 1)
PUBLIC_DOMAIN=bill-generator.${PUBLIC_IP}.nip.io

# 3. Secrets
DB_MONGO_USER=admin
DB_MONGO_PASS=secure_password
DB_POSTGRES_USER=keycloak
DB_POSTGRES_PASS=secure_password
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASS=admin