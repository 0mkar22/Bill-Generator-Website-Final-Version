{
  "name": "lumina-ledger-design-mcp",
  "version": "1.0.0",
  "description": "Unified MCP server and Antigravity tooling for Lumina Ledger",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node ./server.ts",
    "lumina:lint": "node -e \"const fs=require('fs'); const path=require('path'); const code=fs.readFileSync(path.resolve(__dirname, '../../bill-generator-gateway/src/App.jsx'),'utf8'); if(code.includes('#000000') || code.includes('bg-black')){ console.error('Violation: Pure black detected. Lumina requires bg-zinc-950 (#09090b).'); process.exit(1); } else { console.log('Lumina styling validated.'); }\""
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.6.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "typescript": "^5.7.0"
  }
}
